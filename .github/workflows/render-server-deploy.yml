name: Deploy matching server to Render

on:
  push:
    branches: [main]
    paths:
      - 'server/**'
      - 'render.yaml'
      - '.github/workflows/render-server-deploy.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: render-matching
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Trigger Render deploy hook
        env:
          RENDER_DEPLOY_HOOK_URL: ${{ secrets.RENDER_MATCHING_DEPLOY_HOOK_URL }}
        run: |
          set -euo pipefail
          test -n "$RENDER_DEPLOY_HOOK_URL"
          curl --fail --silent --show-error --max-time 30 \
            -X POST \
            "$RENDER_DEPLOY_HOOK_URL"
          echo "Render matching deploy hook triggered"

      - name: Wait for matching health check
        env:
          HEALTH_URL: ${{ secrets.RENDER_MATCHING_URL }}
        run: |
          set -euo pipefail
          test -n "$HEALTH_URL"
          for i in $(seq 1 60); do
            if curl --fail --silent --show-error --max-time 10 "${HEALTH_URL%/}/" > /tmp/health.json; then
              cat /tmp/health.json
              echo "Matching server is healthy"
              exit 0
            fi
            echo "Waiting for matching server... attempt ${i}/60"
            sleep 10
          done
          echo "Matching server health check failed"
          exit 1
